package my.lessons.spring_introduction;

import org.springframework.context.support.ClassPathXmlApplicationContext;


/**
 * <h1>Inversion of Control (IOC)</h1> – передача прав Spring контейнеру на создание управление объектами.<br>
 * Таким образом мы можем в любой момент обратиться к Spring контейнеру, который будет нам возвращать объекты.<br><br>
 * Способы конфигурации Spring Container:<br>
 * - XML file (устаревший способ) {@link XMLConfigWay}<br>
 * - Annotations + XML (современный способ) {@link AnnotationsPlusXMLConfigWay}<br>
 * - Java Code (современный способ)<br>
 *
 *  @author kubarevd
 *  @version 1.0
 * */
public class IoC {
    public static void main(String[] args) {

    }
}
